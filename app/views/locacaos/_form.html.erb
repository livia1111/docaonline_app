<%= form_for(@locacao) do |f| %>
  <% if @locacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locacao.errors.count, "error") %> prohibited this locacao from being saved:</h2>

      <ul>
      <% @locacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_de_retirada %><br />
    <%= f.date_select :data_de_retirada %>
  </div>
  <div class="field">
    <%= f.label :data_de_entrega %><br />
    <%= f.date_select :data_de_entrega %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
